services:
  traefik:
    container_name: traefik
    image: traefik:v3.5.1
    restart: unless-stopped
    ports:
      - "80:80"     # HTTP (redirects to HTTPS)
      - "443:443"   # HTTPS
      - "8080:8080" # Traefik Dashboard (internal only)
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./config/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./config/dynamic.yml:/etc/traefik/config/dynamic.yml:ro
      - traefik_certificates:/etc/traefik/acme
    environment:
      - CLOUDFLARE_DNS_API_TOKEN=${CLOUDFLARE_DNS_API_TOKEN}
      - TZ=${TIMEZONE:-America/New_York}
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik.rule=Host(`traefik.martin-ohana.com`)"
      - "traefik.http.routers.traefik.tls=true"
      - "traefik.http.routers.traefik.tls.certresolver=cloudflare"
      - "traefik.http.routers.traefik.service=api@internal"
      # Global redirect to HTTPS
      - "traefik.http.routers.http-catchall.rule=hostregexp(`{host:.+}`)"
      - "traefik.http.routers.http-catchall.entrypoints=web"
      - "traefik.http.routers.http-catchall.middlewares=redirect-to-https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      - "com.centurylinklabs.watchtower.enable=true"
  
  tar1090-proxy:
    image: traefik/whoami
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tar1090.rule=Host(`tar1090.martin-ohana.com`)"
      - "traefik.http.routers.tar1090.tls=true"
      - "traefik.http.routers.tar1090.tls.certresolver=cloudflare"
      - "traefik.http.services.tar1090.loadbalancer.server.url=http://192.168.1.109/"
      - "traefik.http.routers.tar1090.middlewares=tar1090-stripprefix"
      - "traefik.http.middlewares.tar1090-stripprefix.stripprefix.prefixes=/tar1090"

volumes:
  traefik_certificates:
    driver: local

networks:
  traefik:
    external: true